{
  "name": "physical-ai-humanoid-robotics-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd frontend && npm ci",
    "build": "cd frontend && npm run build"
  },
  "engines": {
    "node": ">=20.0"
  }
}

