{
  "build": {
    "dockerfilePath": "./backend/Dockerfile",
    "buildCommand": "echo 'No custom build command specified, using Dockerfile instructions.'"
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port 8000"
  }
}
