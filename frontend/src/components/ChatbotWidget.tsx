// This is a simplified fallback component in case other references exist
import React from 'react';
import LuxuryChatbotWidget from './LuxuryChatbotWidget';

const ChatbotWidget: React.FC = () => {
  return <LuxuryChatbotWidget />;
};

export default ChatbotWidget;